<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moaadticket.moaadticket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".matches_date" />
        <activity android:name=".StadiumActivity" />
        <activity android:name=".BuyTickets" />
        <activity android:name=".TheTicket" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainTabsActivity" />
        <activity android:name=".AddTaskActivity" />
        <activity android:name=".AutofitRecyclerView" />
        <activity android:name=".MainActivity" />
        <activity android:name=".TaskAdapter"></activity>
    </application>

</manifest>